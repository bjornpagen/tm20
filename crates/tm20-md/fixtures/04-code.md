# Code

Fenced, hung, Menlo. Spaces do not wrap:

```rust
fn measure() -> u16 { 576 }
fn hang(x0: u16) -> u16 {
    x0 + 8
}
```

    indented still counts

Inline `code` inside *emphasis* stays Mono. Prose wraps; code blocks need explicit line breaks to fit the tape.
