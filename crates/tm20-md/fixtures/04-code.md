# Code

Fenced, hung, Commit Mono. Spaces do not wrap:

```
fn measure() -> u16 { 576 }
fn hang(x0: u16) -> u16 { x0 + 8 }
```

    indented still counts

Inline `code` inside *emphasis* stays Mono. This line is meant to overflow the tape: fn overflow(a, b, c, d, e, f) { a + b + c + d + e + f }
