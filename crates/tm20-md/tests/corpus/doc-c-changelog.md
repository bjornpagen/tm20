## 0.4.0

- `compose` hangs notes after the last frame
- autolink https://example.com/0.4 stays italic
- inline `Cut::Mono` is Menlo

## 0.3.0

- tables take two or three columns
- bare www.example.com/0.3 is a GFM autolink
- task items honor `- [x]`

## 0.2.0

- `sheet()` rejects raw HTML
- <https://example.com/0.2> is an autolink, not a tag
- fences clip; they do not wrap
